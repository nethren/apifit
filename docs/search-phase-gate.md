# Search phase completion gate

2026-09-16. User asked to continue until the next phase. Next phase means a separately agreed validation/Postman discussion, not implicit Postman implementation or deployment. Earlier turns made measurable progress but left false-empty AI responses open.

## Current phase requirements

1. Search remains central: API/MCP feature requests and named-product lookups produce real directory candidates, internally sorted. No requirements editor or confirmation gates; no invented APIs, provider-key permissions or reliability claims.
2. Correct the reproduced false-empty GitHub ranking and Context7 interpretation cases, across request variants and APIs as well as MCPs. Preserve explicit exclusions and genuine unrelated-request abstention. Names are discovery evidence, not documentation of capabilities or verified ownership.
3. Keep the wider public MCP index, duplicate handling and free keyword fallback. Check the prior broad corpus for regressions; don't substitute a named-provider-only solution for general project matching.
4. Explanations and briefs remain on demand, goal-centred and plain-language, for both API and MCP sources. Unusable documentation must not spend AI credits or invent functionality. Shortlisting is immediate and free; the handover can be downloaded.
5. Preserve opt-in, source/credential safety, privacy, bounded retrieval and the durable cumulative US$5 budget. No automatic paid retries, model escalation, live API/MCP operations or Postman exports.
6. Verify unit/HTTP behaviour, actual AI outputs for changed prompts, the real browser flow and a production build. Record request failures, missing sources, latency and semantic limitations separately. The next-phase decision needs a requirement-by-requirement audit, not just green tests.

## Debug hypothesis and test sequence

The current search/ranking instructions emphasize capabilities and can incorrectly reject product-finder requests or unfamiliar MCP names. Add an explicit distinction between named lookup and feature matching, without enabling the failed strict-identity experiment. Evaluate prompt changes first; don't silently substitute arbitrary keyword results for an AI rejection.

The engineering-debug skill supplies reproduce/isolate/fix/prevent; engineering-testing-strategy supplies unit, HTTP, live and browser layers. Use the saved two failures for development. Freeze a cross-API/MCP challenge set before changing prompts; compare before/after against identical public metadata. Keep held-out outputs sealed until the change is fixed. Retired fixtures can be regression checks but cannot become a fresh holdout again.

Budget the work within the remaining approved ledger, not by resetting it. Prefer saved-result/offline checks. A single paid run is bounded by the existing sub-cap and stops on transport/budget failures; retain uncertain charges. If the phase gates fail, the goal stays active. Do not relabel partial improvements as phase completion.
